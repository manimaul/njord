charts {
    pgUser = "admin"
    pgPassword = "mysecretpassword"
    pgHost = "localhost"
    pgPort = 5432
    pgDatabase = "s57server"

    host = "0.0.0.0"
    port = 9000

    externalScheme = "http"
    externalHostName = "localhost"
    externalPort = 9000

    chartMinZoom = 0
    chartMaxZoom = 30

    chartSymbols = "symbol_rules.yaml"
    chartSymbolSprites = "rastersymbols-day"

    chartTempData = "/tmp/njord"

    webStaticContent = "../chart_server_fe/build/distributions"
}