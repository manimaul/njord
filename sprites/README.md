# Sprite sheet generation

Generate sprite sheet
```shell
python3 create_sheet.py --svg --theme day,dusk,night
```

Environment setup
```shell
source .venv/bin/activate
pip install pillow
```

Sprite generation requires inkscape in your PATH
```shell
brew install --cask inkscape
# or
sudo apt install inkscape
```
